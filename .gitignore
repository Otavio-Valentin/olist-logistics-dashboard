# Ignore Python temporary files
__pycache__/
*.pyc

# (Optional) If you want to ignore data files to keep the repo light,
# you can add *.csv here. But for your portfolio to work immediately
# when someone downloads it, let's keep the CSVs for now.